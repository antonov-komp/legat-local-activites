# Legat. Юридические адреса (ЕГР)

Activity для БП: `GET /api2/by/address` по УНП.

Выходы: статус, `total`, текстовый список адресов (индекс, СОАТО, даты, признак активности), JSON массива `address`.
