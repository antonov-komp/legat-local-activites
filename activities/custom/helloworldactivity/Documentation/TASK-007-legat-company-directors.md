# TASK-007: Legat — Руководители компании (directors)

**Дата создания:** 2026-04-06 00:00 (UTC+3, Брест)  
**Статус:** Новая  
**Приоритет:** Высокий  
**Исполнитель:** Bitrix24 Программист (Vanilla JS)

## Описание
Создать новое кастомное activity для бизнес-процессов Битрикс24, которое получает сведения о руководителях компании через метод Legat `GET /api2/by/directors` по УНП и возвращает данные в дополнительные выходные поля для дальнейшего использования в шагах БП.

## Контекст
Метод `directors()` формирует информацию на основании трех реестров (ЕГР, финансовая отчетность, ГИАС), поэтому в результатах могут быть дубли по ФИО с разными датами и источниками.  
Нужно добавить отдельное activity по аналогии с уже существующими задачами `data`, `contacts`, `court`, `okved`, `address`, `names`.

Ключ API должен оставаться скрытым от UI:
- приоритетно из `local/legat_api_key.php`;
- fallback: `Option::get('main', 'legat_by_data_api_key')`.

## Модули и компоненты
- `local/activities/custom/legatcompanydirectors/.description.php` — метаданные activity
- `local/activities/custom/legatcompanydirectors/legatcompanydirectors.php` — основной класс activity
- `local/activities/custom/legatcompanydirectors/lang/ru/.description.php` — локализация описания
- `local/activities/custom/legatcompanydirectors/lang/ru/legatcompanydirectors.php` — локализация полей и ошибок
- `local/activities/custom/legatcompanydirectors/README.md` — документация по использованию
- `local/activities/custom/LegatByClient.php` — переиспользуемый клиент Legat
- `local/legat_api_key.php` — источник ключа API

## Зависимости
- Доступность API Legat: `https://api.legat.by/api2/by/directors`
- Наличие ключа API (`key`)
- Наличие УНП (`unp`) во входных данных БП
- Поддержка пагинации через параметр `page`
- Механизм custom activity Битрикс24

## Ступенчатые подзадачи
1. Создать папку activity `legatcompanydirectors` по образцу существующих activity Legat.
2. Настроить `.description.php` (входы/выходы/категория/название).
3. Добавить локализацию (`lang/ru`) для подписей и ошибок.
4. Реализовать входные параметры:
   - `UNP` (обязательный),
   - `PAGE` (необязательный, integer).
5. Реализовать получение API-ключа (файл + fallback Option).
6. Выполнить GET-запрос к `https://api.legat.by/api2/by/directors` с параметрами `key`, `unp`, `page`.
7. Реализовать обработку ошибок: пустой УНП, отсутствие ключа, HTTP-ошибка, невалидный JSON, `error` в ответе API.
8. Преобразовать `directors[]` в нормализованную структуру выходных полей.
9. Добавить служебные поля `REQUEST_STATUS`, `TOTAL`, `PAGE`.
10. Добавить логирование с маскированием чувствительных данных.
11. Обновить README и пример использования в БП.
12. Провести ручное тестирование в конструкторе БП.

## API-методы (Legat)
- `GET /api2/by/directors`
- Полный URL: `https://api.legat.by/api2/by/directors`

Параметры запроса:
- `key` (string, required) — ключ доступа
- `unp` (string, required) — учетный номер (УНП)
- `page` (integer, optional) — страница

Передача ключа:
- GET-параметр `key`, либо
- HTTP Header `key: {$api_key}`.

Пример запроса:
`https://api.legat.by/api2/by/directors?unp=692172505&key=***`

## Набор возвращаемых полей
Верхний уровень ответа:
- `error`
- `total`
- `directors`

По каждой записи `directors[]`:
- `name` — наименование (ФИО руководителя)
- `date_begin` — дата начала в роли руководителя
- `date_end` — дата окончания в роли руководителя
- `date_update` — дата обновления
- `reestr_id` — идентификатор источника данных:
  - `1` — ЕГР
  - `2` — финансовая отчетность
  - `4` — ГИАС
- `active` — признак активности записи (`1` / `0`)

## Пример ответа сервера
```json
{
  "error": null,
  "total": 6,
  "directors": [
    {
      "name": "Катлинский Александр Сергеевич",
      "date_begin": "2020-11-13",
      "date_end": "0000-00-00",
      "date_update": "2024-11-12",
      "reestr_id": 1,
      "active": 1
    },
    {
      "name": "Катлинский Александр Сергеевич",
      "date_begin": "0000-00-00",
      "date_end": "0000-00-00",
      "date_update": "2025-12-30",
      "reestr_id": 4,
      "active": 1
    }
  ]
}
```

## Технические требования
- Платформа: Bitrix24 custom activity.
- Реализация: PHP, без внешних JS-фреймворков.
- Ключ API не хранить в репозитории и не выводить в интерфейс.
- Корректно обрабатывать `0000-00-00` в датах (`date_begin`, `date_end`) как «нет данных» в нормализованном выводе.
- Поддержать множественные записи из разных реестров без потери данных.
- Поле `REQUEST_STATUS` обязательно в любом сценарии (успех/ошибка).

## Предлагаемая структура выходных полей
- `REQUEST_STATUS` — статус выполнения (`Успешно` / сообщение ошибки)
- `TOTAL` — общее количество записей
- `PAGE` — обработанная страница
- `DIRECTORS_ITEMS` — нормализованный список руководителей (массив/JSON)
- `DIRECTORS_ACTIVE_ONLY` — опционально только активные записи (`active = 1`)
- `DIRECTORS_RAW_JSON` — опционально сырой JSON для диагностики

## Критерии приёмки
- [ ] Activity появляется в конструкторе БП и запускается без ошибок.
- [ ] При валидном УНП выполняется запрос к `GET /api2/by/directors`.
- [ ] Обрабатывается необязательный параметр `PAGE`.
- [ ] Корректно возвращаются поля `name`, `date_begin`, `date_end`, `date_update`, `reestr_id`, `active`.
- [ ] Корректно обрабатываются записи с `0000-00-00`.
- [ ] При ошибках API/сети в `REQUEST_STATUS` возвращается понятное сообщение.
- [ ] Ключ API не отображается в UI и логах.

## Тестирование
1. Запуск activity с валидным УНП без `PAGE`.
2. Запуск activity с валидным УНП и `PAGE=2`.
3. Проверка корректности `TOTAL` и состава `DIRECTORS_ITEMS`.
4. Проверка обработки `date_begin/date_end = "0000-00-00"`.
5. Сценарий с пустым/невалидным УНП.
6. Сценарий без ключа API.
7. Сценарий недоступности API (таймаут/5xx).

## История правок
- 2026-04-06 (UTC+3, Брест): Создана постановка задачи для метода `directors()` (документ TASK-007).
